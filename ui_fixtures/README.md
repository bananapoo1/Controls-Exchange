# UI fixture data

These fixtures contain synthetic data used only by `scripts/render_ui.py` to render representative buyer, supplier, admin and search states without requiring a live database.

They must not contain production credentials, customer data or real private commercial information.
